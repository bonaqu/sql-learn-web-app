name: Localize Learning Path

on:
  pull_request:
    branches:
      - bonaqu_projects
    paths:
      - .github/workflows/localize-learning-path.yml

permissions:
  contents: write

concurrency:
  group: localize-learning-path-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  localize:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout same-repository head branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Apply exact route copy migration and remove script
        shell: bash
        run: |
          set -euo pipefail
          node scripts/apply-learning-path-copy.mjs
          rm scripts/apply-learning-path-copy.mjs

      - name: Enforce changed-file allowlist
        shell: bash
        run: |
          set -euo pipefail
          changed=$(git status --porcelain --untracked-files=all | cut -c4- | sort)
          expected=$(printf '%s\n' \
            'scripts/apply-learning-path-copy.mjs' \
            'scripts/validate-learning-path.ts' \
            'src/components/LearningPathPortal.tsx' \
            'src/components/ReadinessExplainer.tsx' \
            'tests/e2e/learning-path.spec.ts' | sort)
          test "$changed" = "$expected"

      - name: Commit only route UI code and contracts
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/apply-learning-path-copy.mjs scripts/validate-learning-path.ts src/components/LearningPathPortal.tsx src/components/ReadinessExplainer.tsx tests/e2e/learning-path.spec.ts
          git commit -m 'Localize Learning Path and readiness explanation'
          git push origin "HEAD:${{ github.event.pull_request.head.ref }}"
