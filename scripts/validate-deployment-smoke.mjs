import { readFileSync } from 'node:fs';

const workflow = readFileSync('.github/workflows/cloudflare.yml', 'utf8');
const smoke = readFileSync('scripts/cloudflare-production-smoke.mjs', 'utf8');
const securityAiSmoke = readFileSync('scripts/security-ai-production-smoke.mjs', 'utf8');
const conceptSmoke = readFileSync('scripts/concept-progress-production-smoke.mjs', 'utf8');
const checkpointSmoke = readFileSync('scripts/checkpoint-production-smoke.mjs', 'utf8');
const capstoneSmoke = readFileSync('scripts/capstone-production-smoke.mjs', 'utf8');
const calibrationSmoke = readFileSync('scripts/assessment-calibration-production-smoke.mjs', 'utf8');
const analyticsSmoke = readFileSync('scripts/learning-analytics-production-smoke.mjs', 'utf8');
const dialectFreeSmoke = readFileSync('scripts/dialect-labs-free-production-smoke.ts', 'utf8');
const core = readFileSync('worker/core.ts', 'utf8');
const workerEntrypoint = readFileSync('worker/entrypoint.ts', 'utf8');
const workerIndex = readFileSync('worker/index.ts', 'utf8');
const curriculumWorker = readFileSync('worker/curriculum.ts', 'utf8');
const capstoneWorker = readFileSync('worker/capstones.ts', 'utf8');
const assessmentWorker = readFileSync('worker/assessment.ts', 'utf8');
const assessmentReportV2 = readFileSync('worker/assessment-report-v2-route.ts', 'utf8');
const analyticsWorker = readFileSync('worker/learning-analytics.ts', 'utf8');
const dialectWorker = readFileSync('worker/dialect-labs.ts', 'utf8');
const dialectRealRoute = readFileSync('worker/dialect-real-engine-route.ts', 'utf8');
const dialectRealAdapter = readFileSync('worker/dialect-real-engine.ts', 'utf8');
const freeWrangler = readFileSync('wrangler.jsonc', 'utf8');
const paidWrangler = readFileSync('wrangler.real-engines.jsonc', 'utf8');
const qualityWorkflow = readFileSync('.github/workflows/quality.yml', 'utf8');

const errors = [];
const requireText = (source, text, label) => {
  if (!source.includes(text)) errors.push(`Missing ${label}: ${text}`);
};
const forbidText = (source, text, label) => {
  if (source.includes(text)) errors.push(`Forbidden ${label}: ${text}`);
};

requireText(core, 'curriculumVersion: 1', 'health curriculum version');
requireText(workflow, 'Deploy Cloudflare Free Stack', 'explicit free-tier workflow name');
requireText(workflow, 'node scripts/cloudflare-production-smoke.mjs', 'production smoke entrypoint');
requireText(workflow, 'node scripts/security-ai-production-smoke.mjs', 'security and AI smoke entrypoint');
requireText(workflow, 'node scripts/concept-progress-production-smoke.mjs', 'concept history smoke entrypoint');
requireText(workflow, 'node scripts/checkpoint-production-smoke.mjs', 'checkpoint smoke entrypoint');
requireText(workflow, 'node scripts/capstone-production-smoke.mjs', 'capstone smoke entrypoint');
requireText(workflow, 'node scripts/assessment-calibration-production-smoke.mjs', 'assessment calibration smoke entrypoint');
requireText(workflow, 'node scripts/learning-analytics-production-smoke.mjs', 'learning analytics smoke entrypoint');
requireText(workflow, 'npx tsx scripts/dialect-labs-free-production-smoke.ts', 'free-tier dialect matrix smoke entrypoint');
requireText(workflow, 'node scripts/mastery-progress-production-smoke.mjs', 'mastery smoke entrypoint');
requireText(workflow, 'node scripts/onboarding-production-smoke.mjs', 'onboarding smoke entrypoint');
requireText(workflow, 'cloudflare-smoke-stage.txt', 'curriculum stage diagnostics');
requireText(workflow, 'cloudflare-security-ai-summary.json', 'security and AI diagnostics');
requireText(workflow, 'cloudflare-concepts-stage.txt', 'concept stage diagnostics');
requireText(workflow, 'cloudflare-checkpoint-stage.txt', 'checkpoint stage diagnostics');
requireText(workflow, 'cloudflare-capstone-stage.txt', 'capstone stage diagnostics');
requireText(workflow, 'cloudflare-assessment-calibration-stage.txt', 'assessment calibration stage diagnostics');
requireText(workflow, 'cloudflare-learning-analytics-stage.txt', 'learning analytics stage diagnostics');
requireText(workflow, 'cloudflare-dialect-stage.txt', 'dialect stage diagnostics');
requireText(workflow, "main: 'worker/entrypoint.ts'", 'scheduled production Worker entrypoint');
requireText(workflow, "compatibility_flags: ['nodejs_compat']", 'Worker Node compatibility');
requireText(workflow, "DIALECT_ENGINE_MODE: 'preview-only'", 'free-tier dialect boundary');
requireText(workflow, 'Deploy Worker, D1/KV and static assets', 'free-tier deployment step');
forbidText(workflow, 'containers:', 'paid Container binding in production');
forbidText(workflow, 'Dockerfile', 'Container image build in production');
forbidText(workflow, 'SANDBOX_TRANSPORT', 'Sandbox transport in free production');
forbidText(workflow, 'real-required', 'paid engine requirement in free production');

requireText(freeWrangler, '"main": "worker/entrypoint.ts"', 'default scheduled Worker entrypoint');
requireText(freeWrangler, '"DIALECT_ENGINE_MODE": "preview-only"', 'default preview-only mode');
forbidText(freeWrangler, '"containers"', 'Containers in default Wrangler config');
forbidText(freeWrangler, '"durable_objects"', 'Sandbox Durable Object in default Wrangler config');
requireText(workerEntrypoint, "import worker from './index';", 'HTTP pipeline delegation');
requireText(workerEntrypoint, 'return worker.fetch(request, env, context);', 'canonical fetch delegation');
requireText(workerEntrypoint, 'await handleScheduledAdminAlerts(controller, env);', 'awaited scheduled alert routing');
forbidText(workerEntrypoint, 'const { waitUntil }', 'ExecutionContext method destructuring');
requireText(paidWrangler, '"containers"', 'separate optional paid profile');
requireText(paidWrangler, '"DIALECT_ENGINE_MODE": "real-required"', 'strict optional paid profile');
requireText(paidWrangler, '"SANDBOX_TRANSPORT": "rpc"', 'optional paid RPC transport');
requireText(qualityWorkflow, 'Build real dialect engine image', 'real-engine Docker CI gate');
requireText(qualityWorkflow, 'Run all PostgreSQL and MySQL contracts', '22-contract CI gate');

requireText(smoke, 'expected: [409]', 'stale-write conflict check');
requireText(smoke, 'expected: [401]', 'revoked session check');
requireText(smoke, "'--yes'", 'non-interactive D1 execution');
requireText(smoke, 'verifyCascade()', 'curriculum D1 cascade verification');
requireText(smoke, 'redactRegistration', 'registration redaction');
requireText(smoke, 'tokenPresent', 'non-secret token evidence');
requireText(smoke, 'curriculumVersion === 1', 'deployed schema propagation check');
requireText(securityAiSmoke, "frame-ancestors 'none'", 'production shell CSP assertion');
requireText(securityAiSmoke, 'access-control-allow-origin', 'untrusted origin denial assertion');
requireText(securityAiSmoke, "headers: { origin: 'https://attacker.invalid' } }, [403]", 'untrusted origin fail-closed status');
requireText(securityAiSmoke, "reason !== 'consent-required'", 'explicit AI consent assertion');
requireText(securityAiSmoke, "source !== 'workers-ai'", 'Workers AI provider assertion');
requireText(securityAiSmoke, 'privateLiteral', 'AI context redaction assertion');
requireText(securityAiSmoke, 'revokedSessionRejected: true', 'session revocation summary');

requireText(curriculumWorker, 'MAX_ANSWERS = 220', 'bounded curriculum answer history');
requireText(conceptSmoke, 'curriculumProgress(144', 'full concept answer round-trip');
requireText(conceptSmoke, 'curriculumProgress(221', 'answer ceiling rejection');
requireText(conceptSmoke, 'expectStatus(rejected, 400', 'invalid concept payload status');
requireText(conceptSmoke, 'verify-valid-state-preserved', 'rejected concept state preservation');
requireText(conceptSmoke, 'expectStatus(revoked, 401', 'concept revoked session check');
requireText(conceptSmoke, "confirm: 'DELETE'", 'concept smoke account cleanup');

requireText(checkpointSmoke, "'/api/checkpoints/reports'", 'checkpoint report endpoint lifecycle');
requireText(checkpointSmoke, 'expected: [403]', 'checkpoint owner mismatch check');
requireText(checkpointSmoke, 'expected: [401]', 'checkpoint revoked session check');
requireText(checkpointSmoke, 'verifyCheckpointCascade()', 'checkpoint D1 cascade verification');
requireText(checkpointSmoke, 'FROM checkpoint_reports', 'checkpoint cleanup query');
requireText(checkpointSmoke, "'--yes'", 'checkpoint non-interactive D1 execution');
requireText(checkpointSmoke, 'checkpointRoundTripVerified', 'checkpoint round-trip summary');

requireText(capstoneWorker, "url.pathname === '/api/capstones/reports'", 'capstone Worker route');
requireText(capstoneWorker, 'Capstone reports are immutable', 'capstone immutability guard');
requireText(capstoneSmoke, "'/api/capstones/reports'", 'capstone endpoint lifecycle');
requireText(capstoneSmoke, 'expected: [409]', 'capstone immutable mutation rejection');
requireText(capstoneSmoke, 'expected: [403]', 'capstone owner mismatch check');
requireText(capstoneSmoke, 'expected: [401]', 'capstone revoked session check');
requireText(capstoneSmoke, 'verifyCascade()', 'capstone D1 cascade verification');
requireText(capstoneSmoke, 'FROM capstone_reports', 'capstone cleanup query');
requireText(capstoneSmoke, "'--yes'", 'capstone non-interactive D1 execution');
requireText(capstoneSmoke, 'idempotent !== true', 'capstone exact replay evidence');
requireText(capstoneSmoke, 'Object.keys(stored.submissionFiles || {}).length !== 3', 'capstone SQL round-trip');

requireText(assessmentWorker, "url.pathname === '/api/assessment/calibration'", 'assessment calibration Worker route');
requireText(workerIndex, 'handleAssessmentReportV2Request', 'strict assessment v2 route ordering');
requireText(assessmentReportV2, "url.pathname !== '/api/assessment/reports'", 'strict calibrated report route');
requireText(assessmentReportV2, 'env.DB.batch([', 'atomic assessment batch');
requireText(assessmentReportV2, 'containsForbiddenEvidenceKey', 'learner SQL privacy guard');
requireText(calibrationSmoke, 'assessment-calibration-reject-forged-telemetry', 'forged telemetry rejection stage');
requireText(calibrationSmoke, 'assessment-calibration-reject-sql-field', 'learner SQL rejection stage');
requireText(calibrationSmoke, 'assessment-calibration-rejected-state-preserved', 'assessment state preservation');
requireText(calibrationSmoke, 'submissionSql:', 'explicit forbidden SQL payload test');
requireText(calibrationSmoke, 'expected: [409]', 'assessment immutable mutation rejection');
requireText(calibrationSmoke, 'expected: [403]', 'assessment owner mismatch check');
requireText(calibrationSmoke, 'expected: [401]', 'assessment revoked session check');
requireText(calibrationSmoke, 'verifyD1Lifecycle', 'assessment D1 lifecycle');
requireText(calibrationSmoke, 'anonymousAggregateSurvivedDeletion', 'anonymous aggregate retention evidence');

requireText(workerIndex, 'handleLearningAnalyticsRequest', 'authenticated analytics route');
requireText(workerIndex, 'x-learning-analytics-contract', 'analytics CORS response header');
requireText(analyticsWorker, "current.sharing !== 'coarse-opt-in'", 'analytics opt-in guard');
requireText(analyticsWorker, 'const MINIMUM_COHORT = 5', 'analytics k=5 suppression');
requireText(analyticsWorker, 'exactKeys(row, ROW_V2_KEYS)', 'analytics row unknown-field rejection');
requireText(analyticsWorker, 'exactKeys(row, ITEM_KEYS)', 'analytics item unknown-field rejection');
requireText(analyticsWorker, 'exactKeys(mastery, MASTERY_KEYS)', 'analytics mastery unknown-field rejection');
requireText(analyticsWorker, 'masteryGroups', 'weekly mastery aggregation');
requireText(analyticsWorker, 'experimentGroups', 'weekly experiment aggregation');
requireText(analyticsWorker, 'suppressedMasteryPeriods', 'mastery cohort suppression');
requireText(analyticsWorker, 'suppressedExperiments', 'experiment cohort suppression');
requireText(analyticsWorker, 'DELETE FROM learning_analytics_snapshots', 'analytics opt-out deletion');
requireText(analyticsSmoke, 'analytics must default off', 'analytics default-off production proof');
requireText(analyticsSmoke, 'forbidden SQL field', 'analytics SQL-field rejection');
requireText(analyticsSmoke, 'unknown mastery field', 'analytics mastery-field rejection');
requireText(analyticsSmoke, 'mastery or experiment round-trip mismatch', 'analytics extended snapshot round-trip');
requireText(analyticsSmoke, 'small module/mastery/experiment cohorts were not suppressed', 'analytics layered cohort suppression proof');
requireText(analyticsSmoke, 'opt-out did not delete snapshots', 'analytics opt-out deletion proof');
requireText(analyticsSmoke, "d1Count('learning_analytics_preferences')", 'analytics preference cascade proof');
requireText(analyticsSmoke, "d1Count('learning_analytics_snapshots')", 'analytics snapshot cascade proof');
requireText(analyticsSmoke, 'revoked analytics session', 'analytics revoked session proof');
requireText(analyticsSmoke, 'forgedMasteryRejected: true', 'analytics strict mastery summary');
requireText(analyticsSmoke, 'allSmallCohortsSuppressed: true', 'analytics layered suppression summary');

requireText(workerIndex, 'handleDialectRealEngineRequest', 'optional paid real route before fallback');
requireText(workerIndex, 'handleDialectLabRequest', 'authenticated dialect fallback route');
requireText(dialectWorker, "url.pathname === '/api/dialect-labs/execute'", 'dialect execute route');
requireText(dialectWorker, "url.pathname === '/api/dialect-labs/progress'", 'dialect progress route');
requireText(dialectWorker, 'HOURLY_EXECUTION_LIMIT = 120', 'preview quota');
requireText(dialectWorker, 'dialectLabManifests.reduce', 'dynamic evidence ceiling');
requireText(dialectWorker, 'validateDialectSqlPolicy', 'shared dialect SQL policy');
requireText(dialectWorker, "PREVIEW_VERIFICATION_MODE = 'ci-reference-preview-v1'", 'preview provenance');
requireText(dialectWorker, 'passed: false', 'preview cannot pass');
requireText(dialectWorker, 'evidenceEligible: false', 'preview cannot create mastery');
requireText(dialectWorker, 'offlinePreview: true', 'preview disclosure');
requireText(dialectWorker, 'ciVerifiedReference: true', 'CI reference disclosure');
requireText(dialectRealRoute, 'DIALECT_REAL_ENGINE_RUNNER_VERSION', 'optional paid runner version contract');
requireText(dialectRealRoute, 'sandboxDestroyed', 'optional paid destroy evidence');
requireText(dialectRealAdapter, "transport: 'rpc'", 'optional paid RPC transport');
requireText(dialectRealAdapter, 'await sandbox.destroy()', 'optional paid cleanup');

requireText(dialectFreeSmoke, "const SERVER_DIALECTS = ['postgresql', 'mysql'] as const", 'both server dialect previews');
requireText(dialectFreeSmoke, 'for (const lab of dialectLabManifests)', 'all published patterns loop');
requireText(dialectFreeSmoke, "value.verificationMode !== 'ci-reference-preview-v1'", 'preview provenance assertion');
requireText(dialectFreeSmoke, 'value.evidenceEligible !== false', 'zero-mastery assertion');
requireText(dialectFreeSmoke, 'allPublishedPatternsPreviewed: true', 'complete preview matrix summary');
requireText(dialectFreeSmoke, 'masteryEvidenceCreated: false', 'zero-mastery summary');
requireText(dialectFreeSmoke, 'verifyCascade()', 'dialect D1 cascade verification');
requireText(dialectFreeSmoke, 'FROM dialect_lab_progress', 'dialect cleanup query');
requireText(dialectFreeSmoke, "'--yes'", 'dialect non-interactive D1 query');
requireText(dialectFreeSmoke, 'SQL leaked into D1 progress', 'dialect SQL privacy assertion');
requireText(dialectFreeSmoke, 'expected: [401]', 'dialect revoked session check');

forbidText(workflow, 'cloudflare-register-payload.json', 'password-bearing diagnostic');
forbidText(workflow, 'cloudflare-delete-payload.json', 'recovery-bearing diagnostic');
forbidText(workflow, 'cloudflare-register.json', 'token-bearing diagnostic');
forbidText(smoke, "writeJson('cloudflare-register.json'", 'raw registration response write');
forbidText(securityAiSmoke, 'token:', 'security AI token diagnostic write');
forbidText(securityAiSmoke, 'recoveryCode:', 'security AI recovery diagnostic write');
forbidText(smoke, "writeJson('cloudflare-delete-payload.json'", 'raw delete payload write');
forbidText(conceptSmoke, 'fs.writeFile(token', 'concept token diagnostic write');
forbidText(conceptSmoke, 'fs.writeFile(recoveryCode', 'concept recovery diagnostic write');
forbidText(checkpointSmoke, "writeJson('cloudflare-checkpoint-register.json'", 'raw checkpoint registration write');
forbidText(capstoneSmoke, "writeJson('cloudflare-capstone-register.json'", 'raw capstone registration write');
forbidText(calibrationSmoke, "writeJson('cloudflare-assessment-calibration-register.json'", 'raw assessment registration write');
forbidText(analyticsSmoke, 'token:', 'analytics token diagnostic write');
forbidText(analyticsSmoke, 'recoveryCode:', 'analytics recovery diagnostic write');
forbidText(dialectFreeSmoke, 'authToken:', 'dialect token diagnostic write');
forbidText(dialectFreeSmoke, 'recoveryCode:', 'dialect recovery diagnostic write');
forbidText(analyticsWorker, 'console.log(payload', 'analytics payload logging');
forbidText(analyticsWorker, 'console.error(payload', 'analytics payload error logging');
forbidText(dialectWorker, 'console.log(sql', 'dialect learner SQL logging');
forbidText(dialectWorker, 'console.error(sql', 'dialect learner SQL error logging');
forbidText(dialectRealRoute, 'console.log(sql', 'real dialect learner SQL logging');
forbidText(dialectRealRoute, 'console.error(sql', 'real dialect learner SQL error logging');

if (errors.length) {
  console.error(`Deployment smoke validation failed with ${errors.length} issue(s):`);
  for (const error of errors) console.error(`- ${error}`);
  process.exit(1);
}

process.stdout.write('Deployment smoke validation passed: Cloudflare Free keeps every learning lifecycle production-tested, analytics is default-off and SQL-free with layered module/item/mastery/experiment k=5 suppression, 22 dialect previews create zero false mastery, and real PostgreSQL/MySQL execution remains mandatory in Docker CI.\n');
